# TODO: Set Home Screen Defaults to Offline and Camera

## Tasks
- [ ] Update HomeScreen.tsx to set default mode to offline (isOnlineMode: false)
- [ ] Update HomeScreen.tsx to set default image source to camera (selectionMode: 'camera')
